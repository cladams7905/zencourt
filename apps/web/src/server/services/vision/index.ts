import { VisionService } from "./service";
import { AIVisionError } from "./errors";

const visionService = new VisionService();

export default visionService;
export { VisionService, AIVisionError };
