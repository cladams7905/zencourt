Video text overlay templates:

- Use only the `simple` template.
- Represent `simple` by providing only:
  - `headline`: required, 1-4 impactful words
  - `accent_top`: null
  - `accent_bottom`: null
- Do not output decorative accent lines for video/reel items.
