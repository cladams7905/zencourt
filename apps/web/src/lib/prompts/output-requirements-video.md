Video output requirements:

- This request is for reel/video content only.
- Do not output carousel content.
- Set `body` to `null`.
- Keep each item optimized for short-form vertical video captions and hooks.
