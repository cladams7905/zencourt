"use client";

import { DBCampaign } from "@shared/types/models";

interface DashboardViewProps {
  initialCampaigns: DBCampaign[];
}

export function DashboardView({ initialCampaigns }: DashboardViewProps) {
  return <></>;
}
